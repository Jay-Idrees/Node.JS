<h1>Users</h1>
{{#if hasUsers}}
    <ul>
        {{#each users}}
            <li>{{this.name}}</li>
        {{/each}}
    </ul>
    {{!-- Note that the spacing here in else is important-the first one especially --}}
{{ else}}
    <h1>No Users Found !</h1>
    {{/if}}